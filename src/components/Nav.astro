<!-- Header -->
<header class="z-10 h-16 w-full sticky">
    <div
        class="h-full w-full px-6 lg:px-20 flex items-center justify-between bg-white shadow-lg"
    >
        <!-- Logo -->
        <a href="/">&#127775;</a>
        <!-- Burger Button -->
        <button
            id="burger-menu"
            type="button"
            class="lg:hidden p-0.5 outline outline-1 outline-gray-600 text-gray-600 rounded-lg"
        >
            <svg
                xmlns="http://www.w3.org/2000/svg"
                fill="none"
                viewBox="0 0 24 24"
                stroke-width="1.5"
                stroke="currentColor"
                class="w-5 h-5"
            >
                <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    d="M3.75 6.75h16.5M3.75 12h16.5m-16.5 5.25h16.5"
                ></path>
            </svg>
        </button>
        <!-- Nav > 1024 px -->
        <nav class="hidden lg:flex lg:space-x-2">
            <div class="py-1 px-2">About</div>
            <div class="py-1 px-2">Portfolio</div>
            <div class="py-1 px-2">Contact</div>
        </nav>
    </div>
    <!-- Nav < 1024 px -->
    <nav
        id="menu"
        class="hidden lg:hidden w-full pb-3 px-6 bg-white shadow-lg rounded-b-xl"
    >
        <div class="py-2 font-medium">About</div>
        <div class="py-2 font-medium border-t-2">Portfolio</div>
        <div class="py-2 font-medium border-t-2">Contact</div>
    </nav>
</header>